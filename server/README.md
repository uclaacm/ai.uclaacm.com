In order to start the server, run `npm install` in the `api` directory to install all server dependencies and then run `npm run dev` to start the server at http://localhost:9000.

Alternatively, `npm run build` can be used to generate the compiled JavaScript files in `./dist`.